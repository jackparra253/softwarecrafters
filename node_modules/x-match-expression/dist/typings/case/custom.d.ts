import { BaseMapper, Case } from "./base";
export declare class CustomCase<E, R> extends BaseMapper<R> implements Case<R> {
    readonly test: (element: E) => boolean;
    readonly mapper: R | ((element: E) => R);
    constructor(test: (element: E) => boolean, mapper: R | ((element: E) => R));
    matches(element: any): boolean;
}
